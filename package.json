{
  "name": "ler.dev",
  "workspaces": [
    "cdk",
    "ty"
  ],
  "scripts": {
    "build": "cd ty && npm run build",
    "deploy": "cd cdk && npm run deploy"
  },
  "dependencies": {
    "@trivago/prettier-plugin-sort-imports": "^3.3.0",
    "prettier": "^2.7.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}
