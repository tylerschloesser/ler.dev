import { BeltPath, Derived } from './types-derived.js'
import { Origin } from './types-origin.js'

export function initBeltPaths(origin: Origin): BeltPath[] {
  const beltPaths: BeltPath[] = []
  return beltPaths
}

export function initTiles(
  origin: Origin,
): Derived['tiles'] {
  return {}
}
